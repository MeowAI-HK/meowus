import ConsoleLayoutShell from "@/components/console-layout-shell";

export default function ConsoleLayout({ children }: { children: React.ReactNode }) {
  return <ConsoleLayoutShell>{children}</ConsoleLayoutShell>;
}
